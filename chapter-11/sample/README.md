Install ogen:
```
go install -v github.com/ogen-go/ogen/cmd/ogen@latest
```

If you can't find the `ogen` binary in your PATH, it might be at `$GOPATH/bin`.

To run it:
```
$GOPATH/bin/ogen sample.yml
```